const body=document.body;const toggles=document.querySelectorAll('.theme-toggle');const saved=localStorage.getItem('hh-theme');if(saved==='dark'||(!saved&&matchMedia('(prefers-color-scheme:dark)').matches))body.classList.add('dark');toggles.forEach(b=>b.addEventListener('click',()=>{body.classList.toggle('dark');localStorage.setItem('hh-theme',body.classList.contains('dark')?'dark':'light')}));
const sections=[...document.querySelectorAll('.observe')],links=[...document.querySelectorAll('.nav-link,.mobile-bar a')];const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.querySelectorAll('.reveal').forEach(x=>x.classList.add('visible'));links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+e.target.id))}}),{rootMargin:'-35% 0px -55%'});sections.forEach(s=>io.observe(s));
let lastY=scrollY;const bar=document.querySelector('.mobile-bar');addEventListener('scroll',()=>{if(!bar)return;const y=scrollY;bar.classList.toggle('hidden',y>lastY&&y>180);lastY=y},{passive:true});
