const body=document.body;const saved=localStorage.getItem('hh-theme');if(saved==='dark'||(!saved&&matchMedia('(prefers-color-scheme:dark)').matches))body.classList.add('dark');document.querySelectorAll('.theme-toggle').forEach(button=>button.addEventListener('click',()=>{body.classList.toggle('dark');localStorage.setItem('hh-theme',body.classList.contains('dark')?'dark':'light')}));document.querySelectorAll('[data-next]').forEach(link=>link.addEventListener('click',event=>event.preventDefault()));
